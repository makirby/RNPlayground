/**
 * # RankingContainer.js
 * Container component for Ranking screen
 * @flow
 */

import Ranking from './Ranking'
import {connect} from 'react-redux'

function mapStateToProps(state) {
  return {}
}

export default connect(mapStateToProps)(Ranking)
